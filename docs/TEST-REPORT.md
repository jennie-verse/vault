# Vault — 테스트 결과 (2026-08-08, 글자 크기 6단계 + docs 추가)

## 이번 변경

- webapp-standard.md 기준 미달 2건 보완: 글자 크기 6단계 조절, `docs/` 4종 문서
- 기존 기능·저장 데이터(IndexedDB, 백업/복원 형식)는 변경하지 않음
- 글자 크기 기본(4단계)은 지금까지 써 온 화면 크기와 동일하게 유지 (사용자 승인 사항)
- `sw.js` 캐시 버전을 `2026.07.31-fix1` → `2026.08.08-fontscale1`로 올림

## 통과 항목 (코드 검토로 확인)

- 인라인 JavaScript 문법 오류 없음 (`node --check`로 확인)
- `<style>` 블록의 중괄호 개수 일치 (195쌍) — CSS 구조 깨짐 없음
- `font-size`를 쓰던 규칙 38곳 중 실제 텍스트 입력 요소(`.search input`, `.field`, `textarea.field`, `.code-tools .csearch input`)는 의도적으로 제외하고 그대로 둠 — 6단계를 낮춰도 iOS Safari 자동 확대(16px 미만 입력창)가 발생하지 않음
- 버튼류(`.btn`, `.seg button`, `.sheet-btn` 등)는 글자만 `calc(...)`로 줄고 `padding`/`min-width`/`min-height`는 원래 px 값 그대로라 44×44px 터치 영역이 축소되지 않음
- Settings 화면에 Text size 6단계 세그먼트 버튼 + Reset 버튼 추가, 선택값은 `Storage.setSetting('fontScale', n)`으로 저장, 새로고침 후에도 `loadPrefs()`가 값을 읽어와 `applyFontScale()`로 재적용

## 실기기에서 직접 확인 필요 (Pending)

- iPhone 세로·가로, iPad 세로·가로에서 1~6단계 전체 확인 — 버튼 겹침, 글자 잘림 여부
- 6px·8px(1~2단계)에서 목록·헤더·시트(sheet)·모달의 레이아웃이 깨지지 않는지
- 17px 이상(6단계)에서 헤더 타이틀·목록 줄임표(ellipsis)가 자연스럽게 잘리는지
- Settings 안 Text size 세그먼트가 6개 버튼일 때 좁은 화면(iPhone SE 등)에서 줄바꿈 없이 들어가는지
- 홈 화면에 이미 설치된 기존 앱에서, 이번 배포 후 캐시가 정상적으로 갱신되고 기존 문서·백업 데이터가 그대로 유지되는지
- GitHub Actions 배포 성공 여부 및 실제 배포 URL에서의 콘솔 오류 0건 확인

## 손대지 않은 부분

- 문서 저장/미리보기 샌드박스, 백업·복원 로직, 코드 검색 등 글자 크기와 무관한 기능은 수정하지 않음

---

# 2026-08-10 — webapp-data 연결 (6단계)

문서 **메타 동기화**와 **저장 기록 이벤트**를 붙였습니다. 백업(C층)은 본문이 담기므로 일부러 넣지 않았습니다.
검사 방법: Node 22 + jsdom + `fake-indexeddb`. 공용 모듈 `shared/v1/sync.js`를 메모리 저장소로 동작하는 가짜 모듈로 바꿔치기해 네트워크 없이 실행했습니다. **앱 코드는 검사용으로 한 줄도 고치지 않았습니다.**

| 스크립트 | 내용 | 결과 |
|---|---|---|
| `vault-sync-test.mjs` | `sync.js` 단위 + CSP·Service Worker·버전 정적 검사 | 83건 통과 |
| `vault-rejoin-test.mjs` | 데이터 유실·본문 유출 재현 시도 | 26건 통과 |
| `events-integration-test.mjs` | vault·grove·loom이 만든 파일을 **atlas·trace 실제 앱**에 먹여 확인 | 45건 통과 |

**vault 관련 154건 통과, 0건 실패.** 스크립트는 `Plan/webapp-data_plan/tests/`에 있습니다. `npm test`는 loom·grove 것까지 일곱 개를 모두 돌리므로 합계는 400건입니다.

## 가장 중요한 확인 — 문서 본문이 올라가지 않는가

Vault의 문서는 개인 HTML 원문입니다. 그래서 이 항목을 여러 경로로 확인했습니다.

- [x] `metaFor()`가 담는 필드는 7개(`id·title·tags·createdAt·updatedAt·lastOpenedAt·sizeBytes`)뿐입니다
- [x] 기기 고유 값(스크롤 위치·즐겨찾기 등)도 담기지 않습니다
- [x] **올라간 파일 전체에 `"content"`라는 키가 없습니다**
- [x] **올라간 파일 어디에도 본문 문자열이 없습니다** — 본문에 표식을 심어 두고 확인
- [x] 이벤트 파일에도, 삭제 표시에도 본문이 없습니다
- [x] Atlas 검색 화면에도 본문이 나타나지 않습니다
- [x] 백업(C층) 코드가 `sync.js`에 아예 없습니다

## 통과 — 데이터 유실 방지

- [x] 앱을 막 열어 목록이 비어 있어도 원격 항목 3개가 그대로 남습니다
- [x] 빈 목록을 직접 올려도(`pushIndex([])`) 원격이 줄지 않습니다
- [x] 받아오기가 올리기보다 먼저 일어납니다 — 호출 순서를 기록해 확인
- [x] 올릴 목록은 화면 상태가 아니라 저장소에서 새로 읽습니다
- [x] 같은 id는 `updatedAt`이 최신인 쪽이 이깁니다
- [x] 두 번, 세 번 돌려도 개수가 늘거나 줄지 않습니다
- [x] 통신 실패·토큰 오류 때 원격이 손상되지 않습니다
- [x] **본문을 올리지 않으므로 원격이 로컬 문서를 지우거나 덮을 길이 아예 없습니다**

## 통과 — 이벤트 · 삭제 · 꺼진 상태

- [x] 파일 이름이 `events/vault.<기기>.<YYYY-MM>.json` 순서 — atlas·trace 정규식에 직접 대조
- [x] 문서당 이벤트는 하나이고, 이름을 바꾸면 `detail`만 갱신됩니다
- [x] `at`은 문서를 저장한 시각입니다
- [x] 지운 문서는 목록에서 빠지지 않고 표시만 붙습니다
- [x] 삭제 표시는 삭제 확인을 누른 그 한 곳에서만 찍힙니다 (앱 전체에서 호출 1개)
- [x] 동기화 코드는 삭제를 추론하지 않습니다
- [x] 동기화는 처음에 꺼져 있고, 꺼진 상태에서 요청이 0회입니다
- [x] **동기화 모듈을 못 받아도 앱이 그대로 뜹니다** — 설정 화면을 그리는 네 함수가 모두 첫 줄에서 막습니다

## 통과 — CSP · 버전

- [x] CSP가 새로 들어갔고 `connect-src`가 `'self' https://api.github.com`입니다
- [x] `object-src none`·`base-uri none`·`form-action none`
- [x] **이미지·글꼴·스타일은 일부러 열어 두었습니다** — 저장 문서는 `iframe.srcdoc`이라 부모 CSP를 물려받으므로, 잠그면 외부 자원을 쓰는 문서가 깨집니다
- [x] `sw.js`의 `VERSION`과 `src/version.js`의 `APP_BUILD`가 같습니다
- [x] 캐시 이름이 올라갔고 `html-vault-` 접두사만 지웁니다
- [x] 새 파일 3개가 캐시 목록에 있고, 공용 모듈은 설치를 막지 않는 선택 캐시입니다
- [x] Settings에 App version이 보입니다

## Pending — 실기기(iPhone)에서만 확인 가능 (2026-08-10 추가분)

- [ ] Settings → Sync에서 기기 이름·토큰을 넣고 켜기
- [ ] 켠 뒤 App version이 `2026.08.10-sync1`인지 확인
- [ ] 문서를 하나 저장한 뒤 Atlas 검색에 그 제목이 나타나는지
- [ ] **저장한 HTML 문서가 예전처럼 정상적으로 보이는지 (CSP를 새로 넣었으므로)** — 특히 외부 이미지나 웹폰트를 쓰는 문서
